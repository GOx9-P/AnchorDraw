# Requirements for Ours: COCO dataloader, SemanticDraw experiments, metrics, and visualization.
#
# Recommended for RTX 4090:
# Install PyTorch with CUDA first, then install this file.
# Example:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r Ours/requirements.txt
#
# Notes:
# - torch/torchvision are intentionally not pinned here to avoid accidentally installing a CPU build.
# - xformers is optional; install only if it matches your local PyTorch/CUDA build.

# Core scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
pillow>=10.0
tqdm>=4.66
einops>=0.7
opencv-python>=4.8
matplotlib>=3.7
seaborn>=0.13
PyYAML>=6.0
jsonlines>=4.0

# COCO loading and mask processing
pycocotools>=2.0.7
scikit-image>=0.22

# Diffusion / Hugging Face runtime
diffusers>=0.29.0
transformers>=4.41.0
accelerate>=0.30.0
huggingface-hub>=0.23.0
safetensors>=0.4.3
peft>=0.11.0
sentencepiece>=0.2.0
protobuf>=4.25

# Metrics and evaluation helpers
torchmetrics>=1.4
torch-fidelity>=0.3.0
lpips>=0.1.4
open-clip-torch>=2.24.0

# Experiment management and reproducibility
omegaconf>=2.3
hydra-core>=1.3
rich>=13.7

# Notebooks / debugging
ipython>=8.20
jupyter>=1.0

# Optional UI dependency for running the original SemanticDraw demos
gradio>=4.0
